import '../entities/direction_result.dart';

class CalculateDirectionUsecase {
  const CalculateDirectionUsecase();

  Future<DirectionResult> call(int profileId, DateTime targetDate) {
    throw UnimplementedError();
  }
}
