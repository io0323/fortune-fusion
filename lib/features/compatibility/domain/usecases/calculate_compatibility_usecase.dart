import '../entities/compatibility_result.dart';

class CalculateCompatibilityUsecase {
  const CalculateCompatibilityUsecase();

  Future<CompatibilityResult> call(int profileAId, int profileBId) {
    throw UnimplementedError();
  }
}
